{
  "name": "test_test",
  "version": "0.0.0",
  "description": "Tests an addon",
  "main": "./test.js",
  "license": "MIT",
  "scripts": {
    "start": "rm -rf node_modules && npm install && mocha"
  },
  "dependencies": {
    "my_addon": "file:../"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
